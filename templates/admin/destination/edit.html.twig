{% extends 'base.html.twig' %}

{% block title %}Edit Destination
{% endblock %}

{% block body %}<br><br>
    <center>
        <h1>Modifier votre Destination</h1>
    </center>
    <div class="container">
        <div class="row">
            <div class="col-sm-11">{{ include('admin/destination/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('admin_destination_index') }}">Retour à la liste</a>

                {{ include('admin/destination/_delete_form.html.twig') }}
            </div>

        {% endblock %}
